# Prototype Versions

## Current Baseline

- `prototype-v0.1/` is the preserved 0.1 baseline.
- `prototype-v0.2/` is the preserved encounter-rule working fork.
- `prototype-v0.3/` is the preserved image fork using gallery card art where available.
- `prototype-v0.4/` is the preserved theatre/Codex fork.
- `prototype-v0.5/` is the preserved rebuilt ribbon-table main-screen fork.
- `prototype-v0.6/` is the preserved animated threshold-popup fork.
- `prototype-v0.7/` is the preserved trolley-dilemma fork with matching stop-motion Dilemma card art.
- `prototype-v1.0/` is the current release candidate and self-contained playable version.

The original `prototype/` folder is left intact for now. Use `prototype-v1.0/` for release testing and deployment. Earlier prototype folders remain local historical forks for comparison.
